# ClimateLens runtime dependencies.
#
# Pins aligned with azureml/env_bert.yml (the AzureML-job source of truth).
# Keep this file and env_bert.yml in lockstep - embedding/topic models are
# only reproducible when torch / sentence-transformers / bertopic versions
# match across environments.

### ML & NLP
scikit-learn==1.5.1  # stopwords
torch==2.2.2         # needed for bertopic
sentence-transformers==3.0.1 # Models for embedding sentences into vectors
tokenizers==0.22.1
transformers==4.57.3
huggingface-hub==0.36.0
umap-learn==0.5.6    # Non-linear dimensionality reduction for visualizations
hdbscan==0.8.38.post1 # Density-based clustering algorithm
bertopic==0.16.4     # Topic modeling using transformers-based embeddings
pynndescent==0.5.13
mlflow==2.14.3       # Model tracking, packaging, and deployment
# cohere               # Topic representations

# Data manipulation
numpy==1.24.3
pandas==2.3.1
scipy==1.15.3
pyarrow>=14.0.0
python-dateutil>=2.8.2

# Configuration / I/O
python-dotenv==1.1.0
pyyaml==6.0.2
regex
joblib
emoji==0.6.0
nltk==3.9.0

# Visualization
matplotlib==3.10.5   # Interactive plotting and visualizations
seaborn==0.13.2
plotly==6.3.0
kaleido==0.2.1
wordcloud==1.9.2
pillow==12.0.0
tqdm==4.67.1

# Dev / tooling (kept together so CI and contributor setups agree)
ruff==0.6.9
pytest==8.3.3
mypy==1.20.0
